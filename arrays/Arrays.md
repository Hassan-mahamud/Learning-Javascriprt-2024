* We create an array called numbers containing five elements.
* We use indexes to access individual elements (arrays in JavaScript are zero-indexed).
* We modify an element by assigning a new value to it.
* We add a new element to the end of the array using the push method.
